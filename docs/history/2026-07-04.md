# 2026-07-04

## Summary

Made the OMP adapter more v1-ready by turning its empty test suite into focused coverage and tightening the recall compactor around the shared fused candidate contract.

Package metadata remains `0.1.0`; the premature `1.0.0` marker was reverted until broader runtime verification proves the v1 contract.

## OMP recall compactor

`C:/Projects/solarisael-house-omp/solarisael-house-proof/recall.ts` now treats `retrievalCandidates` from the core recall result as the primary compact context surface.

Behavior now covered:

- fused retrieval candidates are surfaced with bounded fields and excerpt sizes
- raw semantic/content chunk arrays are suppressed when fused candidates exist
- reverse-canon matches survive only when directly named by term/alias or tied to a surfaced candidate `source_path`
- raw chunk arrays remain available as fallback behavior when fused candidates are absent

This started as runtime-context cleanup; later in the same pass the first core query-routing/source-selection slice landed below.

## OMP adapter test suite

Added Bun tests under `C:/Projects/solarisael-house-omp/tests`:

- `adapter-registration.test.ts`
  - adapter label
  - lifecycle hook registration
  - Solarisael OMP tool names, approval classes, and parameter schema surface
- `recall-compactor.test.ts`
  - fused candidate output
  - raw chunk suppression in fused path
  - reverse-canon filtering


## Core + OMP v1-readiness slices

Added the first runtime-smoke and query-routing slices while keeping package metadata at `0.1.0`:

- core JSON-only memory source switch for deterministic tests
- core pre-turn injection smoke coverage from temp room JSON indexes
- core forced-JSON recall coverage, including multi-entry ranked thread candidates and canon pointer-file matching
- pure core `query-routing.ts` for parser/classifier/lane decisions
- source routing for core memory injection and recall skip flags
- Postgres helper skip flags for semantic/content/date branches in `--mode full`
- OMP auto-recall gating through shared query routing
- OMP context-hook and safe tool execute smoke tests
- OMP `remember`/`sleep`/`wake` tests through temp `SOLARISAEL_SUBSTRATE` fake scripts, with no real substrate writes

Still not v1-proof:

- live OMP process reload/hygiene proof
- real production-substrate write/read proof
- full retrieval acceptance matrix and deeper live query-routing tuning

## Verification

```text
C:/Projects/solarisael-house          bun test -> 46 pass, 0 fail
C:/Projects/solarisael-house          python py_compile helpers -> passed
C:/Projects/solarisael-house-omp      bun test -> 12 pass, 0 fail
```
