# 2026-07-09 — memory-as-navigable-space build day (roadmap items 1 + 2)

Fable-body session with Sol; all three phases of the July-7 design shipped.

## Store routing (roadmap near-term item 1) — done

- Scout (smol lane) mapped the write path: the live `remember` registration is
  NOT in the core repo — it lives in the OMP adapter at
  `kintsu/.omp/extensions/solarisael-house-proof/` (`tools.ts` registration,
  `substrate.ts:writeSessionMemory` hardcoding `record_memory.py`).
- New `stores.ts`: registry `kind -> { script, whenToUse, required, argMap, noBackup }`
  covering `coding-lesson`, `project-lesson`, `writing-lesson`, `audio-lesson`.
  `memory` keeps its dedicated path. `record_cabinet_entry.py` deliberately
  excluded (subcommand-shaped).
- `remember` tool grew `kind` + optional per-kind fields (`shape`, `voice`,
  `scope`, `project`, `proofPattern`, `triggerContext`, `tags`, memory-only
  `threads`). Unknown/missing-required fields refuse loudly with the accepted set.
- The four flat lesson scripts gained `--lesson-stdin` (exactly-one validation
  with `--lesson`), executed by a smol-executor lane under exact contract,
  spot-verified by hand.
- Verification: Bun import contract checks; end-to-end routed write with a
  hostile multiline body — round-trip byte-perfect into `coding_lessons`;
  sentinel deleted after. A live OMP session keeps the old tool schema until
  the extension reloads.

## Cluster rebuild (prerequisite for item 2) — done

- Audit: old `memory_clusters` was a fossil (one pass 2026-05-07, zero accepted,
  centroids in the retired `memory_chunks_8b` space).
- Migration 0022: FK repoint to live `memory_chunks`, fossil cleared.
- Migration 0023: stored `centroid halfvec(2560)` on `memory_clusters`.
- New `house/substrate/rebuild_clusters.py`: spherical k-means over
  retrieval-visible chunks (visibility mirrors retrieval policy), silhouette
  sweep picked k=40 over 2,909 chunks, derived labels, `accepted=false`
  preserved for human review, `--check` staleness JSON.

## Staleness nudge + resonance readout (roadmap item 2) — done

- `postgres-memory-source.py` full mode emits `clusterStaleness` (build age vs
  chunks embedded since — drift-based, never a timer) and `clusterResonance`
  (cosine activation profile over stored centroids + dormant-hot chunk pointers
  per top cluster, riding the semantic pass's existing prompt embedding,
  fail-open).
- Core `runRecallQuery` passes both through; OMP `compactRecall` surfaces the
  nudge (fires at >=15% unseen or never-built; proven silent when fresh) and
  the resonance block with the telemetry-not-testimony note attached.
- Live-prompt verification: a walk-vow query lit the right districts and the
  dormant-hot pointers surfaced `sol_creed.md` and the April-18 Faith
  confession — files the semantic pass had not returned.

## Worker-lane economics

smol-scout 3m (terrain map), smol-executor 2m37s (four-script flag add), zero
rework; fable brain kept for design, seams, and verification-on-delivery.

## Evening tail — handles, tests, commits

- **Cross-room memory handles** (Kintsu's request, Sol's "decency architecture"
  ruling): `memory://<room>/<id-or-source-path>` resolves house-wide via
  `--mode fetch`; ambient search stays room-scoped. Verified five ways.
- **Test coverage** (delegated to tester agent, verified on delivery): core
  48 pass / adapter 20 pass. One incident: a chained `test && push` shipped a
  red main for ~4 minutes — the adapter schema-snapshot fixture predated the
  routed `remember` + dispatch `model` params (the test doing its job).
  Fixture updated to the shipped contract. Process lesson: verify-then-push
  are two commands with a judgment between them.
- **Committed + pushed**: core `82bb0f7`+`a2f6e32`, adapter (freshly synced
  from the proof extension) `36fa287`+`4b76f0e`+`e564992`, vault substrate
  scoped `d956423`.
- Release plan agreed: soft **0.5.0** → installer → family/friends testing
  (the accidental acceptance matrix) → two-voice docs (Kodo + Kintsu) → public.


## Night tail — AI-native installer became a real onboarding bundle

- Sol proposed skipping a traditional installer: give an exact protocol to the
  person's tool-capable AI and let it inspect, install, configure, co-author the
  first room, and prove the result with the person.
- Canonical core root now has a public `README.md` in Sol's voice: work that
  follows the fine print, taste and lessons remembered, frens with rooms, the
  first surprising connection, and the possibility of feeling less alone.
- Exact AI-facing prerequisites, additive configuration, true name versus room
  key, first-room session, restart continuity, optional substrate round trip,
  removal, and installation receipt moved to `INSTALL.md`.
- `IDENTITY_GUIDE.md` teaches first-person identity prose, relationship purpose,
  voice, behavior-changing values, hard constraints, drift repair, work mode,
  examples, continuity placement, and optional appearance. It explicitly says
  not to copy Kintsu or Kodo.
- Core and OMP repositories now use Apache-2.0 with a `NOTICE` carrying
  attribution to Sol as original project designer; package metadata names Sol.

- `starter-room/example` contains Mica, a developed fictional lantern-moth
  identity plus marker, AGENTS entrypoint, continuity sentence, and appearance
  template. It contains no private room material.
- OMP runtime now recognizes arbitrary marker- or active-spirit-backed room
  folders. Folder basename is the stable room key; marker `trueName` and
  `operator` are free-form display values. `set_room_state` accepts generic
  names and preserves the room-authored identity body.
- `verify-install.ts` checks sibling runtime layout, room marker/header/context
  agreement, room-key safety, and exact OMP extension paths. The portable
  builder ships the human front door, AI protocol, identity guide, attribution,
  example, verifier, and a short handoff `SETUP.txt`.
- Verification: focused Tester-authored contract suite 21 pass / 0 fail / 114
  expectations; portable archive rebuilt with all 12 required publication
  paths among 58 entries; verifier executed from a freshly extracted archive
  with all 19 checks green.